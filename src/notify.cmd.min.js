;define('notify/1.0.0/dialog.cmd.min', ['jquery/1.11.1/jquery.cmd.min'], function(require, exports, module){
  var $ = require("jquery/1.11.1/jquery.cmd.min");
	
  var Notify = function(content, options){
	 
    var options = $.extend({
      maskClickable: true, //the layer of bg is clickable
      clickCallback: $.noop, //the callback after click some place of wrapper
      filter : [], // decide what elements are the trigger
      autoTimeout: "infinit", //the delay time before the wrapper destroy,
			initCallback: $.noop,
			containerStyle: {"width": "500px", "height": "300px"},
			hasMask: true
      
    }, options || {})
    
		var self = this
		
		
		
  var notifyProxy= function(content, options){
	   var content = content || '';
    return new Notify(content, options);
  }
  module.exports = notifyProxy;
});